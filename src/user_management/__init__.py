"""
User management app
- Handles local and external authors
    - Local authors are authors that are registered in the system
    - External authors are authors that are not registered in the system, but our node knows about
- Handles external nodes
    - External nodes are other instances of compatible systems that this node knows about (they host external authors)
- Handles user authentication/authorization
- Handles user registration
- Handles user self-service (profile/password/etc)
"""
